# Benchmark Card: Agades LWE Strategy Gym

## Benchmark Type

Toy and downscaled AttackPlan validation and evaluator smoke tests.

## Metrics

- `combined_score`
- `estimated_time_bits`
- `estimated_memory_bits`
- `validity_score`
- MAP-Elites feature dimensions.

## Caveats

Mock-estimator results verify plumbing only. Real estimator results require pinned versions, source mapping, and independent review.

