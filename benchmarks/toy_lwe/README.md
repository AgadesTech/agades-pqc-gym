# Toy LWE Benchmarks

These target configs are for evaluator smoke tests and documentation. They are not security claims.

`agades-lwe benchmark benchmarks/toy_lwe --out runs/toy_benchmark.jsonl` builds public seed AttackPlans from these targets and evaluates them with the configured estimator.

